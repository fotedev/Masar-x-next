{
  "name": "desktop",
  "version": "0.5.6",
  "private": true,
  "type": "module",
  "description": "Masar X desktop app — Electron wrapper that loads the web build over a local HTTP server (research.md §2; spec FR-003, FR-005, FR-006).",
  "scripts": {
    "dev": "echo '[desktop] dev script not yet implemented — will be wired in US1 (Phase 3). See specs/004-multi-platform-expansion/tasks.md.' && exit 1",
    "build": "echo '[desktop] build script not yet implemented — will be wired in US1 (Phase 3). See specs/004-multi-platform-expansion/tasks.md.' && exit 1",
    "typecheck": "tsc --noEmit",
    "lint": "echo '[desktop] no ESLint config yet — scaffold only (T002).'",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  },
  "dependencies": {
    "better-sqlite3": "^11.5.0",
    "electron-updater": "^6.3.9",
    "masarx-shared": "workspace:*",
    "next": "^16.2.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^2.1.8",
    "electron": "^32.2.0",
    "electron-builder": "^25.1.8",
    "typescript": "^5.5.3",
    "vitest": "^2.1.8"
  }
}
