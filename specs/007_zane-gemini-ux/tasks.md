# Tasks: 007_zane-gemini-ux

> Gates for every task: pnpm typecheck · pnpm lint (ratchet 52 warnings) · web vitest 20/20 · no NEW hardcoded-Arabic hits · ar/en key parity. Hard boundary: apps/web + packages/shared messages only; ChatMessageItem.tsx and the owner's uncommitted en/aiAssistant.json Puter→AI rename are NEVER touched/staged (en json partial-staged via HEAD+ours blob).

## 1. Spec landing

- [x] 1.1 specs/007_zane-gemini-ux/spec.md + tasks.md committed before code (9a9f9c6)

## 2. Commit 1 — Full-bleed shell + window-edge scrollbar

- [x] 2.1 Layout.tsx: assistant-route `<main>` → `"relative flex min-h-0 w-full flex-1 flex-col overflow-hidden"`; assistant route bypasses PageTransition (plain `w-full h-full min-h-0` div, like admin/light) (28b8fc5)
- [x] 2.2 page.tsx: root → `flex h-full min-h-0 w-full flex-col` (deviation: `h-full` instead of `flex-1` — the PageTransition-bypass div is a plain block, so flex-1 had no height source; h-full resolves against its `h-full`); all `100dvh − …` calc classes and `max-w-5xl mx-auto` removed (28b8fc5)
- [x] 2.3 ChatContainer.tsx: scroll div `w-full min-h-0` + `dir="ltr"`; inner wrapper `dir={isRTL ? "rtl" : "ltr"}` + `flex min-h-full w-full max-w-4xl mx-auto flex-col` wrapping both state branches; `.chat-messages` retained (28b8fc5)
- [x] 2.4 Verified: scroll container right edge == window width (ar+en, 1920×1000 and 1280×800), hero/chat × mobile/desktop, zero document scroll, footer-lock intact → commit `fix(web): dock zane chat scrollbar to window edge (full-bleed shell)` (28b8fc5)

## 3. Commit 2 — (+) tools popover, bottom model selector, slim header

- [x] 3.1 i18n: `tools` (أدوات زين / ZANE tools) + `model` (النموذج / Model) added to ar+en (orchestrator-owned edit)
- [x] 3.2 ChatInput.tsx: (+) circular button (w-9 h-9 sm:w-10 sm:h-10) → upward popover (overlay + Escape dismissal): Quick Quiz / 3 modes with Check / Summarize (spinner+disabled states) / Clear chat
- [x] 3.3 ChatInput.tsx: model pill beside (+) (menu upward, models array moved here, `setSelectedModel` = page's handleModelChange)
- [x] 3.4 ChatHeader.tsx slim → ultra-minimal transparent bar (owner confirmation): status/title + student toolset (selects/Start/last-exam chip) + AI settings icon; mode/model pills and summarize/clear buttons removed (447 → 145 lines)
- [x] 3.5 page.tsx: props re-wired (ChatInput gains 8 props; ChatHeader drops 7)
- [x] 3.6 Commit `feat(web): zane tools popover, bottom model selector and slim header` (5de6491); en json partial-staged (HEAD + 2 lines only — owner's Puter→AI rename preserved uncommitted)

## 4. Verification & report

- [x] 4.1 Gates: typecheck ✓ (4 projects) · lint ✓ 0 errors / 52 warnings (ratchet unchanged) · tsc after every repair
- [x] 4.2 Browser-probe matrix: popover opens with all 6 items (ar+en, anchored to (+), within viewport) · active-mode Check · summarize disabled without messages · Escape + overlay + item-click dismissal · model switch persisted across reloads (gpt-4o → claude) · mode switch persisted (student/group/programming) · student toolset renders in slim header (student_agent) · scroll flush right edge ar+en · zero document scroll
- [x] 4.3 Execution notes: dispatch-2 agy run hit its quota cap mid-run (resets ~162h) leaving ChatHeader.tsx truncated — orchestrator reconstructed the prologue (interface/signature/modes) from git HEAD and verified the rest; agy stray scratch file `.openclaw-find-region.mjs` moved to `.trash/` (I9). Frozen framer-motion opacities observed during probing were rAF throttling of the hidden IAB pane (env artifact) — states verified correct via DOM + persistence once the pane was foregrounded.

## 5. Polish round (owner feedback after real-browser review, 2026-09-16)

Owner notes: inconsistent element placement · group the 3 personas in their own submenu · (+) menu does not close on far outside click (REAL bug) · composer frame too heavy/grey-blue — wants Gemini · textarea direction must be automatic. Analysis addendum: double mode selector (hero + popover) · header still hosts AI Mode button · model pill too cyan · muted send button unclear.

- [x] 5.1 ROOT CAUSE (outside-click): the `fixed inset-0` overlays were trapped by the card's `backdrop-blur` containing block, so they covered only the card — far clicks never reached them. Replaced with a document `pointerdown` listener + wrapper-ref containment checks (both menus, mutual exclusivity kept, Escape kept). Overlays deleted.
- [x] 5.2 Persona accordion in (+) menu: "نوع المساعد" row (Bot + active-mode badge + chevron) expanding the 3 modes with Check; AI Settings item moved into the menu (opens PuterSettingsModal). New i18n key `assistantPersona` (ar/en; en partial-staged).
- [x] 5.3 Gemini-style composer: `rounded-3xl` 1px neutral border, `bg-white/90 dark:bg-slate-900/70 backdrop-blur-xl shadow-lg`, subtle neutral focus-within border; cyan aura + cyan focus glow deleted; textarea fully transparent.
- [x] 5.4 Auto direction: removed the `isRTL text-right/left` overrides that fought `dir="auto"`; textarea `dir={inputMessage ? "auto" : isRTL ? "rtl" : "ltr"}` — typed content self-directs, empty state follows locale (Arabic placeholder stays right-aligned). Verified: Arabic → rtl, English/code → ltr, empty-ar → rtl.
- [x] 5.5 Consistency: composer width unified `max-w-4xl` in both states; neutral model pill (no cyan badge); clearer disabled send (`bg-slate-200/80 dark:bg-slate-700/60`); hero mode dropdown deleted (ChatContainer −~90 lines, dead imports/state pruned); `aria-expanded` added to both menu buttons.
- [x] 5.6 Verified: outside far-pointerdown closes state (aria-expanded probe) · mutual exclusivity · accordion + settings item · dir matrix · hero without dropdown · composer 864px (4xl) in initial state · mobile 360px popover within viewport · gates green. Commit: `fix(web): zane composer polish (gemini style) — persona submenu, outside-click fix, neutral pill`.

## 6. Centering + SSR boundary round (owner feedback, 2026-09-16)

Owner reported: residual horizontal mis-centering + two console errors (useRouter-in-Server-Components; ThemeScript script-tag warning). Commit: `fix(web): keep zane chat inside PageTransition ssr boundary + symmetric scrollbar-gutter centering` (3168d8a).

- [x] 6.1 **useRouter crash was NOT stale** (correcting the first diagnosis): bisected by worktree file swaps over cold `.next` — the break entered exactly with 28b8fc5's `|| isAssistantRoute` PageTransition bypass. PageTransition is `dynamic(…, { ssr: false })`; rendering the chat page's children in a plain div makes Next 16 evaluate the "use client" page in the RSC layer during SSR → `useRouter` crash. Only ai-assistant breaks (other pages fine). Fix: bypass removed (Layout.tsx, with explanatory comment); full-bleed flex chain preserved because PageTransition's root is already `flex h-full min-h-0 w-full flex-1 flex-col`. Cold-verified: kill node + rm -rf .next + fresh dev → ar 5/5 × 200, en 3/3 × 200, other routes 200.
- [x] 6.2 Centering root cause was twofold: (a) the chat scrollbar (~10px) ate width only on the right; (b) the hero state's "hidden" scrollbar wasn't actually hidden — unlayered `.chat-messages { scrollbar-width: thin }` (index.css) beats Tailwind's layered `[scrollbar-width:none]` utility in the cascade. Fix: `.chat-scrollbar-gutter { scrollbar-gutter: stable both-edges; scrollbar-color: …; scrollbar-width: thin }` applied to the chat-state container only; `scrollbar-width` removed from `.chat-messages` so the hero's hiding utility wins. (Tailwind's arbitrary-property form `[scrollbar-gutter:stable_both-edges]` was not emitted by the JIT — plain CSS class used instead; Chrome supports both-edges, verified inline.)
- [x] 6.3 Numeric verification (1280×800, ar): hero state eaten=0, insets 192/192, message column vs window center = **0px**; chat state gutter applied, insets 192/192, center delta **0px**, flush right edge; hero cards grid now uniform 2×2 (`grid-cols-2`, no `lg:grid-cols-4` squeeze that truncated "Summarize a…").
- [x] 6.4 ThemeScript warning: **rejected** the proposed next/script refactor — gotcha #19/I7 (prior attempt `1d951d5..611a022` caused a CSP-nonce hydration mismatch and was reverted in `d20247f`). Dev-only, load-bearing; file untouched.

## 7. Composer dock / select sizing / scrollbar round (owner feedback, 2026-09-16)

Owner reported (Edge/Windows + 7 screenshots): residual centering offset; a dark-mode-only rectangular band behind the composer that appears only after starting a chat; jarring text zoom on the student selects; Runtime TypeError crash in puter.ts on /quiz-play. Commits: `6e4286c` (puter guard), `fbb70f1` (dock), `58ed40e` (selects), `0063a93` (centering).

- [x] 7.1 **Composer box-in-box (dark, chat state only)**: the chat-state dock carried `bg-white/90 dark:bg-slate-900/90 backdrop-blur-xl border-t` full-width → a lighter band with a top border behind the rounded capsule, visible in dark only (white/90 melts into the light page; the initial state was already transparent). Fix: one transparent dock class in both states — capsule keeps its own bg/border/shadow and floats Gemini-style; dock horizontal padding `p-2.5` → `p-2` to match the header's `px-2`. Computed-style verified in chat state: bg `rgba(0,0,0,0)`, border-top 0px, backdrop-filter none.
- [x] 7.2 **Select focus jump**: `focus:text-base` + `transition-all` (added in `c518734` as an iOS focus-zoom hack) resized 11/12px → 16px on click and clipped the label ("Select Ex..."). Removed per owner decision (Windows Edge is the real test target); `transition-colors` keeps the subtle color transitions. Verified in-browser: computed font-size 12px before and during focus on both selects; no size-affecting classes left. Known-remaining (acknowledged, unstylable): the OS-native option popup renders white in dark mode.
- [x] 7.3 **puter.ts:254 crash**: the capture-phase `window` error listener also receives resource errors whose ErrorEvent has no `message` → the guard itself threw `undefined.toLowerCase()` (the exact Runtime TypeError overlay on /quiz-play). Fix: `String(event.message || '')`. Verified on /ar/quiz-play/7bbf857a…: page renders the quiz, dev portal contains no error text, and synthetic message-less ErrorEvents produce no error overlay.
- [x] 7.4 **Residual centering**: `scrollbar-gutter: stable both-edges` on the messages container alone inset its edges even with no scrollbar — column edges narrower than header/capsule while centers matched (why the owner still saw an offset after round 6's 0px-center fix). Replaced the gutter hack with a real unlayered `.chat-scrollbar-hidden` (`scrollbar-width:none` + `::-webkit-scrollbar{display:none}`) in BOTH states; the old `.chat-messages::-webkit-scrollbar` width/track/thumb rules are gone. Real CSS because layered Tailwind utilities lose to unlayered rules in this cascade (round-6 lesson) — the hero's layered webkit-hiding utilities were losing exactly that fight. Verified numerically (1852×962, dark, rtl, student mode): header/messages/capsule centers all = window center, **0px offset**, with content overflowing (scrollHeight 1240 > clientHeight 678) and the scrollbar hidden; floating-capsule look confirmed by screenshot.

## 8. Hero centering / persona chevron / hero cleanup round (owner feedback, 2026-09-16)

Owner reported (Edge/Windows + 6 screenshots): the offset is still visible; the persona chevron in the tools menu appears intermittently and is clipped on mobile; asked the agent to flag remaining issues itself. Commits: `6fdaca0` (hero centering + pill removal), `4e72a70` (chevron + clear-chat dimming).

- [x] 8.1 **Hero offset was REAL (±112px), not an optical illusion** (correcting the pasted Gemini diagnosis): numeric probe showed the whole hero cluster (avatar, h1, cards grid, CTA) centered at x=814 in en (−112 vs window center 926) and x=1038 in ar (+112) — direction flips with dir — while the composer sat at 0px. Root cause: the hero motion.div had `max-w-2xl w-full` without `mx-auto` (ChatContainer.tsx:196); a max-width-capped child of a stretch-parent flex-col docks to cross-START (left in LTR, right in RTL). `items-center` centers its children, never itself. Round-6's "0px verified" measured the wrapper column only, never the hero block inside it. Fix: `mx-auto`. **Rejected** the pasted "unify cards to max-w-4xl / grid-cols-4" adaptation: after centering, the 672px card column sits exactly on the composer axis (same hierarchy as real Gemini), grid-cols-4 re-introduces the title truncation fixed in round 6. Verified: heroBlock/h1/cards/capsule all 0px offset in en and ar.
- [x] 8.2 **Persona chevron intermittent/clipped**: the pasted diagnosis blamed a missing `shrink-0` on ChevronDown — it already had one. Real cause: persona badge was `whitespace-nowrap` (no truncation) and the label span `flex-1` without `min-w-0`; flexbox cannot shrink either below content (min-width:auto), so long labels overflowed the popover's `overflow-hidden` and the last item (chevron) was pushed out. Fix: badge → `min-w-0 truncate max-w-[120px] sm:max-w-[150px]`, label span → `flex-1 min-w-0`. Verified at 380px with "ZANE AI (Programming)": badge ellipsizes, chevron fully inside the popover (240–254 within 17–273).
- [x] 8.3 **Hero AI Settings pill duplication removed**: the hero CTA (ChatContainer 331–354) opened the same PuterSettingsModal as the tools-menu item. Pill dropped with its props (`onOpenPuterSettings`, `isPuterSignedIn`) and unused Settings/LogIn imports; page.tsx call site cleaned. Verified: 0 AI Settings buttons in the hero, menu item intact.
- [x] 8.4 **Clear chat had no disabled state** (summarize already dimmed correctly — the pasted claim there was outdated): applied the same pattern `disabled={!hasChatData}` + slate dimming + cursor-not-allowed, red hover kept for enabled. Verified: dimmed/not-allowed in the initial state, active once messages exist.
- [x] 8.5 **Rejected/not-reproduced ledger**: light-mode capsule border already exists (`border-slate-200/80`) — no change (MVP Lock pixel debate). Mobile disclaimer clipping in owner screenshots not reproducible: zero vertical overflow at 380×844 and 360×640. Native select popup stays white in dark (OS-rendered).

## 9. Markdown rendering round (owner feedback, 2026-09-17)

Owner screenshot (assistant message with a table + inline formatting next to an external Markdown Live Preview). Commit: `f980647`.

- [x] 9.1 **Inline formatting vanished inside p/li**: both components flattened children via `flattenChildren`, discarding the `<strong>/<em>/<code>` elements react-markdown had already produced. New `renderInlineChildren` passes elements through, LaTeX handling only on text runs.
- [x] 9.2 **Raw `**` leak**: CommonMark rejects `** نص **` (inner spaces) — common in Arabic model output. `repairSpacedBold` trims inner padding, fenced blocks excluded. Verified: 3 spaced-bold samples render as real `<strong>`, 0 `**` in the rendered layer (raw-view layer excluded from the count).
- [x] 9.3 **GFM tables had no components**: added table/thead/th/td — `overflow-x-auto` rounded wrapper with `min-w-[420px]` (mobile scrolls instead of squeezing), per-cell `dir="auto"` (bidi isolation for mixed cells), theme borders. Verified: wrapper present, 12/12 cells dir=auto.
- [x] 9.4 **react-markdown v10 dropped the `inline` prop**: inline code fell into the block path and rendered as a full code box mid-sentence. Block rendering (CodeBlock + language-markdown escape hatch) moved into `pre`; bare `code` is always inline. Verified: inline chips render, block path intact.
- [x] 9.5 **Guest cache wipe race (found while testing, commit `778f627`)**: the persist effect ran while `user` was still `undefined` and deleted the localStorage chat key before the restore effect re-ran on `user=null` — guest chat survival across reload depended on auth timing. Guard: persist skips `user === undefined` (same as restore). Note: numeric-verify each visible cluster — a centered/working container says nothing about the block inside it (round-8 lesson, again).
- [x] 9.6 Persistence strategy answer for the owner (no code): saving to Supabase is already live for authenticated users and the free tier can carry ~5000 users IF capped — capture as **spec 008 draft** (migration + RLS tracking, 100-message load cap + prune, guest cache cap, DB types). `conversation_id` deferred.

## 10. Bubble width / table structure round (owner feedback, 2026-09-17)

Owner relayed a second model's review of the round-9 screenshot. Commit: single `fix(web)` on ChatMessageItem (bubble width + table + em).

- [x] 10.1 **Rejected: "raw `**bold with spaces**` leak"** — not reproducible; live-DOM probe of that exact line: innerText `English cell: bold with spaces and code` with 0 stars and a real `<strong>`. The pasted diagnosis misread rendered bold as raw asterisks. `repairSpacedBold` unchanged (its `\s+` anchors are correct; zero-space pairs parse natively).
- [x] 10.2 **Assistant bubble width**: the row capped both roles at `max-w-[75..88%]` (round-8 content-hugging legacy), squeezing tables into ~60% of the column. Assistant rows now `w-full` (measured 816px = full column minus avatar); user bubbles keep the caps.
- [x] 10.3 **Table structure**: wrapper border+tint, `min-w-[520px]`, thead `bg-slate-100 dark:bg-slate-800/80`, stronger row borders, zebra striping via new `tr` component. Verified computed styles + screenshot.
- [x] 10.4 **Arabic italic emphasis**: `em` = italic + cyan color (synthetic oblique invisible in Arabic fonts). Rejected `font-serif` (font-identity break).

## 11. Markdown pipeline round — ol numbers, highlighting, blockquote (owner feedback, 2026-09-17)

Owner relayed a Gemini/Claude comparison table. Commits: `3de8e27` (deps: remark-breaks, rehype-highlight, highlight.js), `09920f4` (pipeline + components + markers).

- [x] 11.1 **Ordered lists lost numbers — real, but the pasted mechanism was wrong**: the `<ol>` parsed fine (2 ols in the injected fixture); the shared `li` component hardcoded the cyan dot for every list type, erasing numbers. Markers moved to unlayered CSS (`.zane-ul > li::before` dot / `.zane-ol > li::before` CSS counter, cyan bold tabular-nums). The REAL run-on merge is the other confirmed case: a text line followed directly by a list starting with a number ≠ 1 merges into one paragraph (CommonMark: only "1." interrupts a paragraph) — fixed by remark-breaks (single newlines → `<br/>`).
- [x] 11.2 **Syntax highlighting was genuinely absent** (no rehype/prism packages): rehype-highlight + github-dark theme added to MarkdownRendererHeavy; `pre` passes the highlighted spans through to CodeBlock (copy payload stays plain text), and CodeBlock gained a header bar with the language label + always-visible copy button.
- [x] 11.3 **blockquote had no bar** (prose left it 0px): new component with logical `border-s-4` cyan + tinted panel (right in RTL, left in LTR — rejected the pasted hardcoded `border-r`).
- [x] 11.4 **Rejected by live-DOM evidence**: "strikethrough dead" (del parsed + line-through measured), "tasklists fall back to unicode" (real checkbox inputs measured), "** leaks after punctuation/attached" (0 stars across `:**x**`, `(**x**)`, `**x**` fixtures) — only an explicit `del` style and checkbox accent-color were added. Table "minimalist" rework rejected (round-10 owner-approved structure stands; dir=auto already isolates cell BiDi).
- [ ] 11.5 Live visual verification PENDING: the dev-browser session wedged under repeated 10s-aborted navigations (first post-deps compile took 10.1s server-side vs the 10s client cap). Page compiles and serves 200 with the new pipeline; owner refresh on localhost:3000 is the verification. Numeric probes for this round were taken pre-implementation only.
