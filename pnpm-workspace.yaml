# pnpm workspaces for the Masar X monorepo.
packages:
  - "apps/*"
  - "packages/*"

# Approve all build scripts (sharp, esbuild, @swc/core, @parcel/watcher, etc.)
# for this trusted monorepo. Switch to a per-package list later if needed.
dangerouslyAllowAllBuilds: true
