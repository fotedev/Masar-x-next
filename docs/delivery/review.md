# MasarX Delivery — Independent Spot-Check Review

**Date**: 2026-08-31 · **Reviewer**: independent subagent (read-only, no files modified)
**Repo**: `C:\programming\WEB_Development\projects\masarx_next`
**Docs reviewed**: `DELIVERY/masarx-next/{traceability-matrix,verification-report,handover-report}.md`

> ⚠️ Reading note: `…` characters in some captured file lines (e.g. `proces…_KEY`, `masarx…ache`, `${e…(key)}`) are **exec-tool display truncation artifacts, not file content**. Verified by char-code dump (`apps\web\src\app\api\ai-chat\route.ts:29` is exactly `process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY`, LEN=74) and length arithmetic (`rate-limit.ts:306` LEN=97 = `${encodeURIComponent(key)}`; `academic-utils.ts:3/5` LEN=68/57 = `"masarx_user_academic_cache"` / `"masarx_academic_options_cache"`).

## Claim verification table

| # | Claim | Verdict | Evidence (file:line) |
|---|---|---|---|
| 1 | Middleware CSP: nonce-based, no `'unsafe-inline'` in **script-src** in production | **PASS** | `middleware.ts:23-27` (generateNonce via crypto.getRandomValues); `:61` (isProd = NODE_ENV/VERCEL_ENV); `:76` `script-src 'self' 'nonce-${nonce}'` + `'unsafe-eval'` **dev-only** else `'wasm-unsafe-eval'` in prod; grep: `'unsafe-inline'` appears only at `:84` (**style-src**, documented risk acceptance) and in comments `:78,81,82`. Nonce propagated via `x-nonce` `:140,160`; CSP set on all three response paths `:167,178,208`. |
| 2 | Logger: error AND warn forwarded to monitoring when SENTRY_DSN set; console always | **PASS** | `logger.ts:248-261` `error()` → `console.error` always (`:255`) + `sendToMonitoring("error",…)` (`:260`); `:262-268` `warn()` → `console.warn` always (`:263`) + `sendToMonitoring("warning",…)` (`:267`); `sendToMonitoring` `:173-193` POSTs Sentry envelope only when DSN parses (`:179-193`), fire-and-forget fetch `:232-242` with 5s timeout (`:100,160-169`); info/debug dev-only `:269-284`. |
| 3 | Rate limit: 3 tiers (Upstash → Supabase `rate_limits` → in-memory), fail-open; both AI routes await the check | **PASS** | `rate-limit.ts`: Upstash config `:72-76`, pipeline+reconcile `:221-271`; Supabase config `:78-82`, `rate_limits` read/upsert `:290-338`; in-memory Map `:119`, `decideLocally` `:171-191`; tier selection `:346-377` (Upstash preferred `:351-360`, Supabase fallback `:362-374` with structural-error demotion `:364-368`, in-memory + prod warning `:86-99,376`). Fail-open: `logStoreError` "failing open" `:104-112`, catch keeps local decision `:355-358,370-372`; header doc `:2-25`. Consumers: `api/ai/chat/route.ts:61` `await checkAIChatRateLimit(user.id)` (+ `:97` await record), `api/ai-chat/route.ts:65` (+ `:80`). Note: `checkAIChatRateLimit` is synchronous by signature (`rate-limit.ts:383`) — `await` on it is harmless and present. |
| 4 | Zero `localStorage.getItem(` for quiz_attempt/quiz_history/USER_ACADEMIC/login_attempts/signup_attempts runtime keys in apps\web\src | **PASS** (1 documented legacy exception) | Runtime paths are sessionStorage: login `login/page.tsx:47,57` (+ localStorage **removeItem** only `:86,98`); signup `signup/page.tsx:43,54,105,121`; quiz_attempt `useQuizAttempt.ts:132,189,191`; quiz_history `useQuizAttempt.ts:178,182`, `quiz-attempts/page.tsx:82,213`; USER_ACADEMIC `academic-utils.ts:38,55` (`USER_ACADEMIC_CACHE_KEY="masarx_user_academic_cache"`, length-verified). Cleanup utility `storage-cleanup.ts:9-44` removes all five legacy key groups (`:63` getItem is inside `hasOldLocalStorageKeys()` diagnostic only). **Exception (by design)**: `quiz-attempts/page.tsx:93` `localStorage.getItem("quiz_history")` is a one-time legacy migration read — commented `:90-91`, merges then `localStorage.removeItem("quiz_history")` `:102` (also `:214`). Not a runtime storage dependency; self-cleaning. The only other academic localStorage use is the unrelated options cache `masarx_academic_options_cache` (`academic-utils.ts:71,83`, length-verified). |
| 5 | OnboardingModal: no user_metadata reads/writes; uses useUserAcademic + setUserAcademic | **PASS** | `OnboardingModal.tsx:5` `import { useUserAcademic }`; `:19` `const { academic, departments: userDepartments, setUserAcademic } = useUserAcademic();`; write via `:93` `await setUserAcademic({…})`. `user_metadata` appears only in comments (`:18,35,92` — "no longer read or written"). Zero `localStorage`/`sessionStorage`/`getItem`/`setItem` matches in the file. |
| 6 | Drawer: `w-[min(320px,85vw)]` and `pb-[max(1.5rem,env(safe-area-inset-bottom))]` in MobileNav.tsx | **PASS** | `MobileNav.tsx:130` (drawer panel class, exact strings) and `:162` (scroll area padding). |
| 7 | Tailwind `primary.foreground` mapping; `--primary-foreground` in :root AND .dark | **PASS** | `tailwind.config.js:25` `primary: { foreground: 'hsl(var(--primary-foreground))' }` (inside `extend.colors`). `index.css`: `:root` block at `:43` defines `--primary-foreground: 210 40% 98%` at `:55`; `.dark` block at `:101` defines `--primary-foreground: 0 0% 100%` at `:113`. (Note: an earlier `:root` also exists at `:6`.) |
| 8 | ai-chat SSE: edge fn has acceptsSse + text/event-stream + [DONE]; proxy forwards Accept + streams unbuffered | **PASS** | `supabase/functions/ai-chat/index.ts`: `acceptsSse` defined `:118-123`, used `:335`; `'Content-Type': 'text/event-stream'` `:108`; `sseFrame('[DONE]')` `:185,214`. Proxy `api/ai-chat/route.ts`: Accept forwarded `:95-100` (`'Accept': acceptHeader` in upstream fetch headers); SSE passthrough `:110-120` returns `new Response(response.body, …)` (raw ReadableStream, no buffering) with `Content-Type: text/event-stream`, `Cache-Control: no-cache, no-transform`, `Connection: keep-alive`. |

## Deferral spot-checks (traceability matrix §Deferred summary)

| Deferral | Verdict | Evidence |
|---|---|---|
| `docs/perf/baselines.md` exists | **CONFIRMED** | `Test-Path` → True. |
| `supabase/tests/rls-verify.sql` exists | **CONFIRMED** | `Test-Path` → True. |
| gitleaks binary not installed locally | **CONFIRMED** | `Get-Command gitleaks` → not found on PATH. |
| `apps\desktop\node_modules\masarx-shared` dangling junction | **NOT REPRODUCIBLE — docs stale** | The path is now a **real directory** (no ReparsePoint; LinkType empty), resolves True, contains `package.json` (name `masarx-shared`, version **0.5.6 = packages/shared 0.5.6**) plus `src/`, `tsconfig.json`, `.gitignore`. The dangling-junction issue (handover Known issue #1 / Deferred #5) appears already repaired (e.g. by a later install) or no longer present at review time. |
| (bonus) Shared `typecheck` blocked by dangling `typescript` symlink (handover Known issue #2) | **NOT REPRODUCIBLE — likely repaired** | `packages\shared\node_modules\typescript` is a SymbolicLink that **resolves** (its `package.json` is readable). Typecheck itself not run (read-only review). |

## Inconsistencies found (report-only, nothing fixed)

1. **Stale known-issue/deferral (junction)**: handover Known issue #1 + Deferred #5 describe `apps\desktop\node_modules\masarx-shared` as a dangling junction needing `rmdir` + reinstall. At review time it is a real, resolving directory whose `package.json` matches `packages/shared` (0.5.6). Docs should be refreshed; the `rmdir` fix must NOT be run against a real directory.
2. **Known issue #2 likely stale too**: the `packages/shared` `typescript` symlink resolves; the "dangling symlink" root cause could not be reproduced (typecheck not executed by this review).
3. **Claim-4 wording nuance**: `localStorage.getItem("quiz_history")` still exists at `quiz-attempts/page.tsx:93` as an intentional, self-removing legacy migration read (docs describe this cleanup design; matrix line "real-key cleanup" matches). Strict "zero `getItem`" is false; strict "zero runtime localStorage dependency" is true.
4. **Academic options cache remains in localStorage** (`masarx_academic_options_cache`, `academic-utils.ts:71,83`) — outside the five claimed runtime keys and not claimed otherwise by the docs, but worth knowing since the cleanup utility does not cover it.
5. **Doc/repo drift note**: `verification-report.md` line 29 artifact — none; env var is `NEXT_PUBLIC_SUPABASE_ANON_KEY` (char-code verified). No other doc-vs-repo contradictions found in the sampled claims; all eight claims match the docs' descriptions.
